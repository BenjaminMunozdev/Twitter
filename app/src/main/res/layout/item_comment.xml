<!-- Archivo: app/src/main/res/layout/item_comment.xml (Asegúrate de que sea similar a esto) -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingStart="16dp"> <!-- Un poco de indentación para que se vea como una respuesta -->

    <TextView
        android:id="@+id/txtCommentUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Usuario del Comentario"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtCommentContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Este es el texto de la respuesta al post." />

</LinearLayout>
